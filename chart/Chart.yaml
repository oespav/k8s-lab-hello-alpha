apiVersion: v2
name: hello-alpha
description: hello-alpha, a multi-version helloworld service owned by team-alpha. Attaches an HTTPRoute to a shared platform gateway.
type: application
version: 1.0.0
appVersion: "1.0"
